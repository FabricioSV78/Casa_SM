import {useMemo,useState} from 'react'
import {Link} from 'react-router-dom'
import {Download,Plus,Search,Trash2} from 'lucide-react'
import {useApp} from '../context/AppContext'
import {datePE,money} from '../utils/format'
import {Empty,PageHeader} from '../components/ui'
export default function MovementsPage(){const {movements,tenants,properties,removeMovement,isAdmin}=useApp(),[search,setSearch]=useState(''),[type,setType]=useState('todos');const rows=useMemo(()=>movements.filter(x=>x.estado==='activo'&&(type==='todos'||x.tipo===type)&&`${x.concepto} ${x.categoria} ${tenants.find(t=>t.id===x.inquilinoId)?.nombre||''}`.toLowerCase().includes(search.toLowerCase())).sort((a,b)=>b.fechaMovimiento.localeCompare(a.fechaMovimiento)),[movements,search,type]);const download=()=>{const csv=['Fecha,Tipo,Concepto,Método,Monto',...rows.map(x=>[x.fechaMovimiento,x.tipo,`"${x.concepto}"`,x.metodoPago,x.monto].join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download='movimientos.csv';a.click()};return <><PageHeader title="Movimientos" description="Todos los ingresos y gastos registrados" action={isAdmin&&<Link className="btn-primary" to="/movimientos/nuevo"><Plus size={18}/> Nuevo</Link>}/><div className="card mb-5 grid gap-3 p-4 sm:grid-cols-[1fr,180px,auto]"><label className="relative"><Search className="absolute left-3 top-3.5 text-stone-400" size={19}/><input className="field !pl-10" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar movimiento..."/></label><select className="field" value={type} onChange={e=>setType(e.target.value)}><option value="todos">Todos</option><option value="ingreso">Ingresos</option><option value="gasto">Gastos</option></select><button className="btn-secondary" onClick={download}><Download size={18}/> CSV</button></div>{!rows.length?<Empty/>:<div className="card overflow-hidden"><div className="hidden overflow-x-auto md:block"><table className="w-full text-left"><thead className="bg-stone-50 text-sm text-stone-500"><tr>{['Fecha','Tipo','Persona / concepto','Espacio','Método','Monto',''].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y divide-stone-100">{rows.map(x=><tr key={x.id}><td className="whitespace-nowrap px-4 py-4">{datePE(x.fechaMovimiento)}</td><td className={`px-4 py-4 font-bold capitalize ${x.tipo==='ingreso'?'text-income':'text-expense'}`}>{x.tipo}</td><td className="px-4 py-4"><b>{tenants.find(t=>t.id===x.inquilinoId)?.nombre||x.persona||'—'}</b><p className="text-sm text-stone-500">{x.concepto}{x.periodosRelacionados&&` · ${x.periodosRelacionados}`}</p></td><td className="px-4 py-4">{properties.find(p=>p.id===x.propiedadId)?.codigo||'—'}</td><td className="px-4 py-4">{x.metodoPago}</td><td className={`px-4 py-4 font-bold ${x.tipo==='ingreso'?'text-income':'text-expense'}`}>{money(x.monto)}</td><td className="px-4 py-4">{isAdmin&&<button onClick={()=>confirm('¿Eliminar este movimiento?')&&removeMovement(x.id)} className="rounded-lg p-2 text-stone-400 hover:bg-red-50 hover:text-red-600"><Trash2 size={18}/></button>}</td></tr>)}</tbody></table></div><div className="divide-y divide-stone-100 md:hidden">{rows.map(x=><div key={x.id} className="p-4"><div className="flex justify-between gap-3"><div><p className="font-bold">{x.concepto}</p><p className="text-sm text-stone-500">{datePE(x.fechaMovimiento)} · {x.metodoPago}</p></div><b className={x.tipo==='ingreso'?'text-income':'text-expense'}>{x.tipo==='ingreso'?'+':'−'} {money(x.monto)}</b></div></div>)}</div></div>}</>}
